{
  "name": "discord",
  "bin": "./bin/discord",
  "systemHint": "You have access to Discord through the `discord` CLI tool (source at ~/Workspace/Exocortex/external-tools/discord-cli). It supports named accounts; use `discord accounts` to list them and `discord <command> -a <alias> ...` to select one. Mutating commands require an explicit account when multiple logins exist. Run `discord -h` for usage reference.",
  "display": {
    "label": "Discord",
    "color": "#5865F2"
  },
  "shell": {
    "literalArgs": [
      { "subcommand": "send", "kind": "tail" },
      { "subcommand": "reply", "kind": "tail" },
      { "subcommand": "edit", "kind": "tail" },
      { "subcommand": "dm", "kind": "flag", "flag": "--send" }
    ]
  },
  "daemon": {
    "command": "env DISCORD_ACCOUNT=\"$(.venv/bin/python3 -c 'from src.accounts import supervised_notify_alias; print(supervised_notify_alias() or \"\")')\" .venv/bin/python3 -m src.notify __daemon__",
    "restart": "always"
  }
}
